# nfpm example configuration file
#
# check https://nfpm.goreleaser.com/configuration for detailed usage
#
name: "realm-admin"
arch: "amd64"
platform: "linux"
version: 0.0.1
epoch: 1
section: "default"
priority: "extra"

provides:
  - realm-admin
depends:
  - postgresql
  - postgresql-contrib

maintainer: "Mix dJoker <mix.djoker@gmail.com>"
description: |
  The set of the VPNGen Data Center managers
vendor: "VPNGen"
homepage: "http://vpngen.org"

contents:
  - src: ./.tmp
    dst: /usr/bin/
    file_info:
      mode: 0700
      owner: root
      group: root
scripts:
  # Preinstall - scritp that set Env varibles
  # preinstall: ./debpkg/preinstall.sh
  # Postinstall - the sensitive data cleaning scritps
  # postinstall: ./debpkg/postinstall.sh
    #preremove: ./scripts/preremove.sh
    #postremove: ./scripts/postremove.sh
deb:
  compression: xz
